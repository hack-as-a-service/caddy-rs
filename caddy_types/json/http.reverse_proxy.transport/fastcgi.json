{"type": "struct", "type_name": "github.com/caddyserver/caddy/v2/modules/caddyhttp/reverseproxy/fastcgi.Transport", "struct_fields": [{"key": "root", "value": {"type": "string", "doc": "Use this directory as the fastcgi root directory. Defaults to the root\ndirectory of the parent virtual host."}, "doc": "Use this directory as the fastcgi root directory. Defaults to the root\ndirectory of the parent virtual host."}, {"key": "split_path", "value": {"type": "array", "elems": {"type": "string", "doc": "The path in the URL will be split into two, with the first piece ending\nwith the value of SplitPath. The first piece will be assumed as the\nactual resource (CGI script) name, and the second piece will be set to\nPATH_INFO for the CGI script to use.\n\nFuture enhancements should be careful to avoid CVE-2019-11043,\nwhich can be mitigated with use of a try_files-like behavior\nthat 404s if the fastcgi path info is not found."}}, "doc": "The path in the URL will be split into two, with the first piece ending\nwith the value of SplitPath. The first piece will be assumed as the\nactual resource (CGI script) name, and the second piece will be set to\nPATH_INFO for the CGI script to use.\n\nFuture enhancements should be careful to avoid CVE-2019-11043,\nwhich can be mitigated with use of a try_files-like behavior\nthat 404s if the fastcgi path info is not found."}, {"key": "resolve_root_symlink", "value": {"type": "bool", "doc": "Path declared as root directory will be resolved to its absolute value\nafter the evaluation of any symbolic links.\nDue to the nature of PHP opcache, root directory path is cached: when\nusing a symlinked directory as root this could generate errors when\nsymlink is changed without php-fpm being restarted; enabling this\ndirective will set $_SERVER['DOCUMENT_ROOT'] to the real directory path."}, "doc": "Path declared as root directory will be resolved to its absolute value\nafter the evaluation of any symbolic links.\nDue to the nature of PHP opcache, root directory path is cached: when\nusing a symlinked directory as root this could generate errors when\nsymlink is changed without php-fpm being restarted; enabling this\ndirective will set $_SERVER['DOCUMENT_ROOT'] to the real directory path."}, {"key": "env", "value": {"type": "map", "map_keys": {"type": "string"}, "elems": {"type": "string", "doc": "Extra environment variables."}}, "doc": "Extra environment variables."}, {"key": "dial_timeout", "value": {"type": "int", "type_name": "github.com/caddyserver/caddy/v2.Duration", "doc": "The duration used to set a deadline when connecting to an upstream.\n\n\nDuration can be an integer or a string. An integer is\ninterpreted as nanoseconds. If a string, it is a Go\ntime.Duration value such as `300ms`, `1.5h`, or `2h45m`;\nvalid units are `ns`, `us`/`\u00b5s`, `ms`, `s`, `m`, `h`, and `d`."}, "doc": "The duration used to set a deadline when connecting to an upstream.\n\n\nDuration can be an integer or a string. An integer is\ninterpreted as nanoseconds. If a string, it is a Go\ntime.Duration value such as `300ms`, `1.5h`, or `2h45m`;\nvalid units are `ns`, `us`/`\u00b5s`, `ms`, `s`, `m`, `h`, and `d`."}, {"key": "read_timeout", "value": {"type": "int", "type_name": "github.com/caddyserver/caddy/v2.Duration", "doc": "The duration used to set a deadline when reading from the FastCGI server.\n\n\nDuration can be an integer or a string. An integer is\ninterpreted as nanoseconds. If a string, it is a Go\ntime.Duration value such as `300ms`, `1.5h`, or `2h45m`;\nvalid units are `ns`, `us`/`\u00b5s`, `ms`, `s`, `m`, `h`, and `d`."}, "doc": "The duration used to set a deadline when reading from the FastCGI server.\n\n\nDuration can be an integer or a string. An integer is\ninterpreted as nanoseconds. If a string, it is a Go\ntime.Duration value such as `300ms`, `1.5h`, or `2h45m`;\nvalid units are `ns`, `us`/`\u00b5s`, `ms`, `s`, `m`, `h`, and `d`."}, {"key": "write_timeout", "value": {"type": "int", "type_name": "github.com/caddyserver/caddy/v2.Duration", "doc": "The duration used to set a deadline when sending to the FastCGI server.\n\n\nDuration can be an integer or a string. An integer is\ninterpreted as nanoseconds. If a string, it is a Go\ntime.Duration value such as `300ms`, `1.5h`, or `2h45m`;\nvalid units are `ns`, `us`/`\u00b5s`, `ms`, `s`, `m`, `h`, and `d`."}, "doc": "The duration used to set a deadline when sending to the FastCGI server.\n\n\nDuration can be an integer or a string. An integer is\ninterpreted as nanoseconds. If a string, it is a Go\ntime.Duration value such as `300ms`, `1.5h`, or `2h45m`;\nvalid units are `ns`, `us`/`\u00b5s`, `ms`, `s`, `m`, `h`, and `d`."}], "doc": "Transport facilitates FastCGI communication.\n", "module_inline_key": "protocol"}